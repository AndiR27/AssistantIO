quarkus:
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  hibernate-orm:
    database:
      generation: drop-and-create  # Recrée la base à chaque test
    log:
      sql: true
  log:
    category:
      "org.hibernate.SQL":
        level: INFO
